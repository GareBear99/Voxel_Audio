# Optional Description Policy

The Description field is optional.

## Behavior

- Blank Description = render no description text.
- Typed Description = render the typed text.

This keeps the export layout clean when the user does not want any descriptive text.

## Compact layout

When blank, the export uses a compact visualizer capsule instead of rendering empty description text.
